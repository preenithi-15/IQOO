services:
  - type: web
    name: natura
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python -m app.ui.web_dashboard"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
    healthCheckPath: /
